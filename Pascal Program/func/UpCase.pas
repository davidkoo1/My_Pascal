//UpCase -> Запусти на lazarus<-если что скопируй и вставь, а то...то работает, то нет.
//UpCase преобразует с такого в ТАКОГО
var s : string;
begin
s := 'hello WOrld';
writeln(s);
writeln(UpCase(s));

writeln('the end. Press  ENTER...');
readln;
end.


