program prob;
uses crt;
var a, b, c : Integer;
begin
  
      
      read(a);
      if (odd(a) = true) // or a mod 2 = 0 ) 
      then 
      writeln('любит')
      else
      WriteLn('not любит');
end.
